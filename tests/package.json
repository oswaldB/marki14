{
  "name": "marki14-tests",
  "version": "1.0.0",
  "description": "Playwright tests for marki14 application",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --headed",
    "test:debug": "PWDEBUG=1 playwright test --headed",
    "test:setup": "playwright test --global-setup",
    "test:report": "playwright show-report",
    "install": "playwright install"
  },
  "dependencies": {
    "@playwright/test": "^1.44.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}