# Fichier d'exemple pour les variables d'environnement
# Copiez ce fichier en .env et configurez les valeurs appropriées

# Configuration de la base de données PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=votre_mot_de_passe
DB_NAME=postgres
DB_SSL=false

# Configuration Parse Server (pour compatibilité)
PUBLIC_APPLICATION_ID=marki
PUBLIC_JAVASCRIPT_KEY=Careless7-Gore4-Guileless0-Jogger5-Clubbed9
PUBLIC_SERVER_URL=https://dev.parse.markidiags.com

# Configuration SFTP pour les PDFs
FTP_HOST=votre_serveur_sftp
FTP_PORT=2222
FTP_USERNAME=votre_utilisateur
FTP_PASSWORD=votre_mot_de_passe

# Configuration SMTP par défaut (pour les tests)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASSWORD=motdepasse
SMTP_EMAIL=noreply@example.com
SMTP_USE_SSL=false

# Configuration du serveur
PORT=3000
HOST=0.0.0.0

# Configuration Ollama (pour la génération d'emails avec IA)
OLLAMA_HOST=http://localhost:11434